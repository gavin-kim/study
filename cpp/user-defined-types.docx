new: allocate memory from an area called the free store (dynamic memory and heap)
delete: objects are alive until they are destroyed using delete
Objects allocated on the free store are independent of the scope from which they are created.
